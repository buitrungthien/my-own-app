import React from 'react';
import './styles.scss';

const sideBar = props => (
    <nav className='sidebar'>
        SideBar
        <ul>
            <li></li>
            <li></li>
        </ul>
    </nav>
);

export default sideBar;