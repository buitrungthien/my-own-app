import React from 'react';
import './styles.scss';

const header = props => {
    return (
        <header className='header'>
            <nav>
                Header
            </nav>
        </header>
    );
};

export default header;