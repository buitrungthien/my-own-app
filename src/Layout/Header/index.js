import React from 'react';
import './styles.scss';

const header = props => {
    return (
        <header className='header'>
            <input></input>
            <nav>
                Header
            </nav>
        </header>
    );
};

export default header;